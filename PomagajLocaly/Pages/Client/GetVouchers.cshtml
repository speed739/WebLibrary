@page
@model PomagajLocaly.Pages.Client.GetVouchers

@{
    ViewData["Title"] = "Buy offer";
}
<h1>@ViewData["Title"]</h1>


<div>
    <h4>Offers</h4>
    <hr/>
    <table class="table table-bordered">
        <thead class="thead-dark">
        <tr>
            <th>Name </th>
            <th>Description </th>
            <th>Price </th>
            <th>Action </th>
        </tr>
        </thead>
        <tbody>
        @foreach (var offer in Model.Offers)
        {
            <tr>
                <td>@Html.DisplayFor(model => offer.Name) </td>
                <td>@Html.DisplayFor(model => offer.Description)</td>
                <td>@Html.DisplayFor(model => offer.Price)</td>
                <td>
                    <a asp-page="./AddOffer" asp-route-id="@offer.OfferId">Add</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>